cc_binary(
    name = "vecdb_cli",
    srcs = ["main.cpp"],
    deps = [
        "//src/metric:distance",
        "//src/storage:vector_store", # <- 新增
        "//src/utils:utils",          # <- 新增
    ], 
)